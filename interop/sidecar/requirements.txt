# Pinned to the exact versions in .venv-interop — the image IS the version
# contract (spec 7): the conformance suite's numbers are only reproducible
# against these builds.
chainladder==0.9.2
pandas==2.3.3
numpy==2.4.6
fastapi==0.139.2
uvicorn==0.51.0
