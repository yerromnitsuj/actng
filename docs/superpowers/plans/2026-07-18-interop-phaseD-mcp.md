# Interop Phase D: The MCP Layer — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** The governed workspace exposed over MCP with the staged-write policy: external AI clients read everything, mutate nothing directly, and reach changes ONLY through the same gated promotion path humans use — with truthful actor recording. Spec authority: rev 2.1 section 8 (entire) + 13 Phase D acceptance.

**Grounding:** docs/research/interop/mastra-mcp-capabilities.md (source-verified): MCPServer from @mastra/mcp (NEW dependency — add the current line, ~1.14+; the design needs neither mapAuthInfoToUser nor FGA), tools/agents exposable (agents become ask_<key>, description REQUIRED), startHTTP streamable transport with serverless option, auth via req.auth -> context.mcp.extra.authInfo, and the FAIL-OPEN caveat this plan's helper + self-test close.

## Global Constraints
(Master + spec rev 2.1 section 8. The exposure policy is a SECURITY policy: no direct mutation tools over MCP, ever; requireMcpTenant fail-closed; boot self-test proves the failure mode; actor defaults to "external-mcp-client" and lands verbatim in the ledger.)

### Task D1: MCP helpers in @actuarial-ts/agents
src/mcp.ts: `requireMcpTenant(context, key="projectId")` — reads context.mcp?.extra?.authInfo (fall back to requestContext.get("mcp.extra") per the documented universal pattern), THROWS AgentsError("NO_TENANT_CONTEXT") when authInfo or the key is absent (the defineActuarialTool wrapper envelopes it); `assertFailClosed(server, probeToolId)` — the boot self-test helper: calls the probe tool through the server's executeTool WITHOUT auth info and asserts the envelope failure, throwing loudly if the tool succeeded (a missed wire-up cannot boot). @mastra/mcp added (verify the installed version's MCPServer/executeTool signatures against its d.ts FIRST — house rule; record the pinned line in the report). Tests: helper behavior with stub contexts; the self-test both directions (fail-closed passes; a deliberately-unguarded probe makes it throw).

### Task D2: workbench MCP server (apps/server)
src/mcp/workspaceMcp.ts: MCPServer({ name: "actng-workspace", version, tools, agents: { advisor: advisorAgent } }) exposing: READ tools (get_workspace_overview, analyze_development_factors, assess_data_quality, get_analysis_results, get_diagnostic_detail, run_sensitivity, crosscheck_with_python — reuse the EXISTING tool objects where their input schemas are MCP-safe; every exposed tool's execute path resolves tenant EXCLUSIVELY via requireMcpTenant — where existing tools read tenantOf(requestContext), WRAP them in MCP-specific variants that bridge authInfo -> the requestContext the underlying tool expects, do NOT fork tool logic) + exactly TWO write-shaped tools: stage_study (input: the StudyDoc JSON; starts the workbench promotion via startPromotion; returns the intake gate view) and advance_promotion (runId + gate + decision payload incl. rationale/attestation for gate 3 + REQUIRED actor defaulting "external-mcp-client"; drives advancePromotion; the Gate-2 hard block and PROMOTION_BUSY flow through unchanged). NO patchWorkspace/set_*/run_analysis direct mutations exposed — a test asserts the exposed tool list exactly matches the policy allowlist. HTTP: mount at /mcp on the express app via startHTTP; bearer-token middleware (ACTNG_MCP_TOKEN env; absent env = MCP DISABLED entirely, log once) validating and setting req.auth = { projectId } from a token->project mapping (v1: single token grants the projectId named in ACTNG_MCP_PROJECT_ID; document the single-tenant simplification). Boot self-test wired into server startup when MCP is enabled (assertFailClosed on a probe read tool); startup ABORTS if it fails.
Tests (server): exposed-list-matches-policy; read tool through executeTool with authInfo works and without fails enveloped; stage_study -> advance through all four gates via executeTool with actor recorded ("external-mcp-client" default and explicit values verbatim in the ledger note); direct-mutation absence; disabled-when-no-token.

### Task D3: recipes + acceptance
docs/interop/mcp-notebook-recipe.md: connecting Claude (or any MCP client) to the workspace server from a notebook context — config JSON, the read->stage->advance flow, what the actor/attestation fields mean, the accountability model (verbatim quote of the ledger truth: the mechanism records WHO decided, it cannot verify humanness). E2E acceptance test (server): an "external client" walk purely through the MCP surface completing a promotion (per spec 13 Phase D) + the no-auth probe failing closed + a mutation-tool-reach attempt failing (tool not found). Cross-restart: a promotion staged via MCP resumes after restart (reuse the existing proof pattern — extend or parameterize the promotion restart scripts).

### Task D4: review + gates + /ship
Review lens: MCP security (token handling, authInfo bridging, the allowlist, self-test integrity, prompt-injection posture of ask_advisor over MCP — the advisor has ACTION tools registered; decide + enforce whether ask_advisor over MCP gets the read-only advisor variant instead [RECOMMENDED: expose a drafting/read-only advisor assembled without action tools] and document). Gates: all suites both shores + CI green. Master log + /ship.
